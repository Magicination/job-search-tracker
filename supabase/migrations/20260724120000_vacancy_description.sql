-- Полный текст описания вакансии — букмарклет теперь передаёт его вместе
-- с остальными полями, чтобы дальше можно было анализировать частотность
-- слов/навыков в описаниях у откликов с разной конверсией (см. секцию
-- "Слова в вакансиях" на странице Аналитика).
alter table public.applications add column if not exists vacancy_description text;
